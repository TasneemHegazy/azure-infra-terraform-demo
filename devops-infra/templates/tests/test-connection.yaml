apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-infra.fullname" . }}-test-connection"
  labels:
    {{- include "devops-infra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-infra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
